services:
  _defaults:
    autowire: true
    autoconfigure: true

  # Commands
  event4u\DataHelpers\Frameworks\Symfony\Command\MakeDtoCommand:
    arguments:
      $projectDir: '%kernel.project_dir%'
    tags:
      - { name: 'console.command' }

  event4u\DataHelpers\Frameworks\Symfony\Command\DtoTypeScriptCommand:
    arguments:
      $projectDir: '%kernel.project_dir%'
    tags:
      - { name: 'console.command' }

